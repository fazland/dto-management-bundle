index:
    path: /
    defaults: { _controller: 'AppBundle:Test:index' }

protected:
    path: /protected
    defaults: { _controller: 'AppBundle:Test:protected' }

unavailable:
    path: /unavailable
    defaults: { _controller: 'AppBundle:Test:unavailable' }

underscored:
    path: /underscored
    defaults: { _controller: 'AppBundle:Test:underscored' }

camelized:
    path: /camelized
    defaults: { _controller: 'AppBundle:Test:camelized' }

camelized_public:
    path: /camelized_public
    defaults: { _controller: 'AppBundle:Test:camelizedPublic' }

semver:
    path: /semver/{_version}
    defaults: { _controller: 'AppBundle:Test:semver' }
